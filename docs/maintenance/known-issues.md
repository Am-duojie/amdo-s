# 已知问题与修复建议（开发记录）

## 管理端回收订单管理筛选问题
现象：页面可能存在：
- `paymentFilter` 未定义导致运行时报错
- 打款筛选参数未传（后端为 `payment_status`）
- 将 `paid` 当成订单流程状态导致筛选无结果

建议修复（最小改动）：
1. 定义 `paymentFilter` 变量
2. 请求参数传 `payment_status`
3. 将 “已打款 paid” 从“订单状态”筛选移除，只保留在“打款状态”筛选

请以最新代码为准核对。
