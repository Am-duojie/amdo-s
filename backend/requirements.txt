Django==5.2.8
djangorestframework==3.16.1
django-cors-headers==4.9.0
channels==4.3.2
channels-redis==4.3.0
Pillow==12.0.0
PyMySQL==1.1.2
requests==2.32.3
beautifulsoup4==4.12.3
tenacity==9.0.0
tqdm==4.67.1
playwright==1.48.0

# 生产环境部署相关
daphne==4.1.0  # ASGI服务器，支持WebSocket（推荐用于生产环境）
gunicorn==21.2.0  # WSGI服务器（如果不需要WebSocket可以使用）
python-decouple==3.8  # 环境变量管理（可选，推荐）
